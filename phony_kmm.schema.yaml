# Rime schema
# encoding: utf-8

schema:
  schema_id: kmm
  name: 伪方案-kmm
  description: 用于词典部署

engine:
  translators:
    - table_translator

translator:
  dictionary: kmm
  enable_user_dict: false
  enable_sentence: false
  enable_encoder: false
